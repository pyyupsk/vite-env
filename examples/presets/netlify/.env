# No Netlify vars needed locally — the preset detects the platform via
# NETLIFY=true and validates its vars as optional when not on Netlify.
DATABASE_URL=postgres://user:password@localhost:5432/app
VITE_APP_NAME=vite-env netlify preset example

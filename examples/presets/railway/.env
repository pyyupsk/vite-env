# No RAILWAY_* vars needed locally — the preset detects the platform via
# RAILWAY_ENVIRONMENT_ID and validates its vars as optional when not on Railway.
DATABASE_URL=postgres://user:password@localhost:5432/app
VITE_APP_NAME=vite-env railway preset example

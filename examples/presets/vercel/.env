# No VERCEL_* vars needed locally — the preset detects the platform via
# VERCEL=1 and validates its vars as optional when not on Vercel.
DATABASE_URL=postgres://user:password@localhost:5432/app
VITE_APP_NAME=vite-env vercel preset example

# react — React SPA

React + Vite with the Zod path. `virtual:env/client` imported straight into components with full type inference (`env.VITE_DEBUG` is `boolean` in JSX).

```bash
bun run --filter @vite-env/example-react dev
```
